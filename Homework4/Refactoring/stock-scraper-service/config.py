class Config:
    """ Configuration for the Flask app and services """
    SECRET_KEY = 'your_secret_key'  # Update with a real secret key
    # Scheduler configuration
    SCHEDULER_API_ENABLED = True
